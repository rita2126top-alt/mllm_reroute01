# @package _global_
# LLaVA-1.5-7B + FastV K=3 vanilla physical_delete @ avg_T = 128.
# avg_T = (3*576 + 29*0.1418*576) / 32 = 128.0
defaults:
  - /eval: pope
  - /model: llava15_7b_hf
  - _self_

routing:
  method: fastv
  action: physical_delete
  scoring_layer: 3
  keep_ratio: 0.1418

eval:
  batch_size: 1
  log_samples: true
