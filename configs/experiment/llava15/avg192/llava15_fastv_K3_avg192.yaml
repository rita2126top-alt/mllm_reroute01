# @package _global_
# FastV K=3 at avg_T = 192.
# Vanilla physical_delete — single decision at L=3, keep_ratio=0.2644.
# avg_T = (3*576 + 29*0.2644*576) / 32 = 192.0
defaults:
  - /eval: pope
  - /model: llava15_7b_hf
  - _self_

routing:
  method: fastv
  action: physical_delete
  scoring_layer: 3
  keep_ratio: 0.2644

eval:
  batch_size: 1
  log_samples: true
