# Pinned versions for CUDA 12.8 / py3.10. For other CUDA, swap the torch
# index URL at the bottom of this comment block.
#
# Usage:
#   conda create -n reroute_release python=3.10 -y && conda activate reroute_release
#   git clone --branch v0.7.1 --depth 1 https://github.com/EvolvingLMMs-Lab/lmms-eval.git
#   pip install -e ./lmms-eval
#   pip install -r requirements.txt
#   bash lmms_eval_patches/apply.sh
#
# Other CUDA: replace the torch index URL with the matching wheel:
#   https://download.pytorch.org/whl/cu121  (CUDA 12.1)
#   https://download.pytorch.org/whl/cu124  (CUDA 12.4)
# and reinstall:
#   pip install --upgrade --force-reinstall torch==2.11.0 \
#       torchvision==0.26.0 --index-url <url>

# ── Core ──
--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.11.0+cu128
torchvision==0.26.0+cu128

# ── Hydra config system ──
hydra-core==1.3.2
omegaconf==2.3.0

# ── HF transformers stack ──
transformers==5.4.0
tokenizers==0.22.2
safetensors==0.7.0
accelerate==1.13.0
huggingface-hub
sentencepiece==0.2.1
protobuf==5.29.6

# ── Vision / image ──
pillow==12.1.1
einops==0.8.2
timm==1.0.26

# ── Numerical / data loading ──
numpy==2.2.6
scipy==1.15.3
fsspec==2026.2.0
datasets==4.8.4

# ── Eval harness side-deps (lmms-eval itself is editable, see header) ──
decord
antlr4-python3-runtime>=4.9,<4.10

# ── Profiler (OPTIONAL — only for TFLOPs reproduction; requires nvcc) ──
# To enable:
#   conda install -c nvidia cuda-nvcc=12.4 -y
#   pip install deepspeed==0.14.5
# Accuracy reproduction works without deepspeed.
# deepspeed==0.14.5
